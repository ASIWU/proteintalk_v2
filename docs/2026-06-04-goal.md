- 搜索策略：
- stage1：同一候选参数同时跑 exp01、exp04、exp05；exp04 使用 no-MSE 脚本，exp05 使用 graph zero，其余参数与 exp01 相同。
- stage1 screen 候选：`base`, `mse050`, `mse075`, `mse100`, `mse050_drop010`, `mse050_drop020`, `mse075_drop010`, `mse100_drop010`, `mse050_lr1e4`, `mse050_lr3e4`, `mse050_warmdecay`, `mse050_pre1`,
`mse050_pre2`, `mse050_target_pdi`, `mse050_target_ppi`。
- stage2 exp03 候选：`base`, `covdrop010`, `drop010`, `drop020`, `bs128`, `lr1e4`, `lr3e4`, `ctrl_drop010`, `covunk_cell`, plus focused `covdrop010_drop010`, `covdrop010_drop020`, `covdrop010_bs128`,
`covdrop010_lr1e4`, `covdrop010_lr3e4`。
- stage3 exp02 候选：同 exp03，但 `covunk_celltype` 替代 `covunk_cell`。
- stage4 exp06 候选：`base`, `drop010`, `drop020`, `bs128`, `lr1e4`, `lr3e4`, `dbl_mse010`, `dbl_mse050`, `rank005`, plus focused `drop020_lr1e4`, `drop020_lr3e4`, `drop020_bs128`,
`drop020_mseinactive010`, `drop020_mseinactive050`；所有 exp06 候选固定保留 `PAIR_FUSION_MODE=dual`, `PAIR_TYPE_FEATURES=1`, `USE_DDI=1`, `GRAPH_PAIR_ADD_SCALE=0.5`。
- 每个 stage 从 screen 结果 promote top 3 到 full 5-fold；若第 3 名和第 4 名 score/AUPRC 差值小于 `0.002`，最多 promote 到 top 5。

- Final selected run：
- 使用 prefix `20260604_cell_llm_clip10_tuned_selected_v1`。
- exp01/04/05 使用 stage1 full 5-fold 最优参数。
- exp02、exp03、exp06 分别使用各自 full 5-fold AUPRC 最优参数。
- exp07 all-data 使用 exp01 selected 参数；reference epoch 从 final exp01 5 folds 的 best epochs 取 mean + nearest rounding，`max_epochs=selected_epoch+1`，用 `last.ckpt` 推理 extra single。
- exp08 all-data 使用 exp06 selected 参数；reference epoch 从 final exp06 5 folds 的 best epochs 取 mean + nearest rounding，`max_epochs=selected_epoch+1`，用 `last.ckpt` 推理 extra double。

## Test Plan

- 静态检查：
- `python -m py_compile train.py infer.py scripts/report_cell_drug_time_eval.py scripts/report_cell_llm_clip10_param_search.py`
- `bash -n scripts/ptv3_experiment_common.sh scripts/run_cell_llm_clip10_param_search.sh`
- Screen 验收：
- stage1 每个候选都有 exp01/04/05 的 3-fold manifests。
- stage2/3/4 每个候选都有对应任务的 3-fold manifests。
- tuning report 输出 stage1 score、两个 gap、exp01 AUPRC，以及 exp02/03/06 AUPRC 排名。
- Full 5-fold 验收：
- promoted candidates 全部完成 5 folds。
- selected 参数来自 full 5-fold，不从 screen 直接定稿。
- Final suite 验收：
- exp01-exp06 各 5 folds；exp07/exp08 各 1 个 all-data run。
- 所有 final manifests 包含 `cell_llm_mode=frozen`、`cell_llm_summary.embedding_rows=74`，且不包含 `cell_type_llm`。
- exp05 final manifests 为 `graph_feature_mode=zero`；其他 final exp 为 `real`。
- 生成 final report：`logs/20260604_cell_llm_clip10_tuned_selected_v1_cell_drug_dose_time_eval.md`、`outputs/...csv`、`outputs/...json`。
- 更新 `docs/2026-06-02_llm_dose_graphallowed_selected_results.md`、`docs/2026-04-15_data_standardization_session_summary.md`，并在 `data/review_summary/` 新增带 HKT 小时分钟的 review 文件。

## Assumptions

- 本轮只调训练超参和已存在的小型 loss/regularization 开关；不改模型结构、不重建 raw/training-ready 数据。
- Tuning 主指标使用 original fold AUPRC；final report 同时保留 original、cell-drug-dose-bylasttime、cell-drug-dose-avgtime。
- exp08 不使用 exp01 参数；按用户修正，exp08 使用 exp06 selected 训练参数和 exp06 reference epoch。
- 若某个 promoted candidate 已有完整、同 prefix、同 config、同 fold manifest，可复用；否则重新训练，避免混入旧 invalid cell-type LLM 结果。
